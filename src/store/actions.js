import * as types from './mutations-types'

const actions = {
    // [types.SET_LOADING_ASYNC]({commit}, config) {
    //     let {time, isLoading} = config;
    //     setTimeout(() => {
    //         commit('SET_LOADING', isLoading)
    //     }, time)
    // }
}

export default actions;

